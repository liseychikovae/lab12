# Домашнее задание lab12

Лабораторная работа Лисейчиковой Екатерины

## Что требовалось сделать

Домашнее задание относится к теме лабораторной работы XII: итоговое оформление серии лабораторных работ.

## Выполненные пункты

- финальная структура показывает весь путь проекта от CMake до публикации артефактов;
- CI использует Ubuntu, что соответствует Linux-совместимой проверке;
- документация по Docker, Vagrant и ngrok остается внутри репозитория;
- итоговый отчет перечисляет, какие навыки закрыты каждой лабораторной.

## Как проверить домашнее задание

```bash
cmake -S . -B _build -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTS=ON
cmake --build _build
ctest --test-dir _build --output-on-failure
bash scripts/package_release.sh
```

## Пояснение по реализации

Проект оформлен так, чтобы его можно было перенести в Linux-окружение и собрать стандартными командами CMake. Файлы не завязаны на абсолютные пути macOS, персональные секреты не добавлены в git, а дополнительные инструменты подключаются через конфигурационные файлы конкретной лабораторной.

## Итог

Домашнее задание выполнено и находится внутри репозитория `lab12` вместе с лабораторной работой.
